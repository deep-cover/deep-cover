require "spec_helper"

RSpec.describe AnotherComponentGem do
  it "does something useful" do
    expect(AnotherComponentGem.hi).to eq(:there)
  end

  it "isn't complete" do
    expect(1).to eq(2)
  end
end
